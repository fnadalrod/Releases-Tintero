import React from 'react'
import { ScrollView, Text, Button, View } from 'react-native'

const Tintero = (props) => {
    return (
        <ScrollView>
            
        </ScrollView>
    )
}

export default Tintero
